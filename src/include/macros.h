
#pragma once

#define PORT "8080"

#define KB (1024)
#define MB (1024 * KB)
#define GB (1024 * MB)

#define MAX(x, y) (((x) > (y)) ? (x) : (y))
#define MIN(x, y) (((x) < (y)) ? (x) : (y))

#define STATIC_PATH_STR "static/"